use16		      ;Генерировать 16-битный код
org 100h	      ;Программа начинается с адреса 100h
 
    mov cx,n	     ;Счётчик повторений цикла
    mov ax,3
    mov bx,3
metka:
    mul bx
loop metka	  ;Команда цикла


    mov ax,4C00h      ;\
    int 21h	      ;/ Завершение программы
;-------------------------------------------------------
n db 3
press:
    db 13,10,'Press any key...$'
